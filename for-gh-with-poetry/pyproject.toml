[project]
name = "file-reader-api"
version = "0.1.0"
description = ""
authors = [
    {name = "Will",email = "yzj_000@163.com"}
]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi (>=0.104.0)",
    "uvicorn (>=0.24.0)",
    "pydantic (>=2.5.0)"
]

[tool.poetry]

[tool.poetry.group.dev.dependencies]
pytest = ">=7.4.0"
pytest-asyncio = ">=0.21.0"
httpx = ">=0.25.0"
requests = ">=2.31.0"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
